_base_: ./residual_diffusion_x0_scale_0.5_long_cosine.yaml

interaction_oracle:
  mode: static_summary
  num_trace_points: 5
  conflict_distance_m: 5.0
  conflict_temperature_m: 1.0

experiment:
  name: static_oracle_interaction_summary
  type: ablation
  cache_from: main_location_split
  note: training-only oracle static interaction summary upper-bound
  sample:
    enabled: false
  analyze:
    enabled: false
