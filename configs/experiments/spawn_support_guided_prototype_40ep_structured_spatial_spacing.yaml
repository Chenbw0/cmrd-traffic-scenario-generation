_base_: ./spawn_support_guided_prototype_100ep_structured_spatial_only.yaml

training:
  num_epochs: 40

experiment:
  name: spawn_support_guided_prototype_40ep_structured_spatial_spacing
  type: ablation
  note: structured spatial-only prototype adaptation with pair-structure preservation, 40 epoch diagnostic run
  train:
    enabled: true
    resume: false
    num_epochs: 40
