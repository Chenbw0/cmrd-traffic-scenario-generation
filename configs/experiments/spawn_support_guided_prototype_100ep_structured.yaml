_base_: ./spawn_support_guided_prototype_40ep.yaml

training:
  num_epochs: 100

experiment:
  name: spawn_support_guided_prototype_100ep_structured
  type: ablation
  note: support-guided prototype spawn with prototype-preserving spatial adaptation and kinematic completion, 100 epoch run
  train:
    enabled: true
    resume: false
    num_epochs: 100
