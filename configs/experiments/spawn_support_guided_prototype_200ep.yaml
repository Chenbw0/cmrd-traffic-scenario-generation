_base_: ./spawn_support_guided_prototype_40ep.yaml

training:
  num_epochs: 200

experiment:
  name: spawn_support_guided_prototype_200ep
  type: ablation
  note: main long-run support-guided prototype spawn with prototype-conditioned residual refinement
  train:
    enabled: true
    resume: false
    num_epochs: 200
