_base_: ./spawn_support_guided_prototype_200ep.yaml

spawn_losses:
  plan_summary_weight: 0.0
  plan_occupancy_weight: 0.0
  scene_summary_weight: 0.0

experiment:
  name: spawn_support_guided_prototype_200ep_no_plan_aux
  type: ablation
  note: removes explicit scene-plan auxiliary supervision to test whether prototype support alone is sufficient

