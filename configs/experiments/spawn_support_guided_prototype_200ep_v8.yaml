_base_: ./spawn_support_guided_prototype_40ep.yaml

training:
  num_epochs: 200

experiment:
  name: spawn_support_guided_prototype_200ep_v8
  type: ablation
  note: v8 long-run support-guided prototype spawn with prototype-bank-v3 and single-prototype local refinement
  train:
    enabled: true
    resume: false
    num_epochs: 200
