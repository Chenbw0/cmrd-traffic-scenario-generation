_base_: ./spawn_support_guided_prototype_200ep.yaml

spawn_prototypes:
  residual_position_scale_m: 0.0
  residual_speed_scale_mps: 0.0
  residual_heading_scale_rad: 0.0

experiment:
  name: spawn_support_guided_prototype_200ep_prototype_copy
  type: ablation
  note: prototype-only floor; decoder cannot move states away from selected prototype

