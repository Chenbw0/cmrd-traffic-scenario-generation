_base_: ./spawn_support_guided_prototype_40ep.yaml

training:
  num_epochs: 100

experiment:
  name: spawn_support_guided_prototype_100ep_hybrid
  type: ablation
  note: support-guided prototype hybrid spawn with deterministic scene compiler, 100 epoch run
  train:
    enabled: true
    resume: false
    num_epochs: 100
