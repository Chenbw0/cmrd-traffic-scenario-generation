_base_: ./main_location_split.yaml

experiment:
  name: ablation_full_history_generation
  type: ablation

generator_input:
  use_history: true
  history_mode: full_history
  require_history_at_sample: true
