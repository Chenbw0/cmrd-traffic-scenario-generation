_base_: ./residual_diffusion_x0_scale_0.5_long_cosine.yaml

interaction_oracle:
  mode: dynamic_trace
  num_trace_points: 5
  conflict_distance_m: 5.0
  conflict_temperature_m: 1.0

experiment:
  name: dynamic_oracle_interaction_trace
  type: ablation
  cache_from: main_location_split
  note: training-only oracle dynamic interaction trace upper-bound
  sample:
    enabled: false
  analyze:
    enabled: false
